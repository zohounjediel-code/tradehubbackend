# Copie ce fichier en ".env" (même dossier) et remplis tes propres valeurs.
# Le fichier ".env" n'est jamais commité sur git (voir .gitignore) --
# c'est fait exprès, ce sont des secrets personnels.

# --- Base de données PostgreSQL (obligatoire) ---
# En dev : crée un projet gratuit sur https://neon.tech, copie la
# "Connection string" (elle contient déjà ?sslmode=require).
# En prod (Railway) : injectée automatiquement par l'addon Postgres.
DATABASE_URL=

# --- Cloudinary (stockage des images produit et preuves de paiement) ---
# Crée un compte gratuit sur https://cloudinary.com, les 3 valeurs
# ci-dessous sont visibles directement sur le dashboard.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# --- Unsplash (photos produit réalistes) ---
# Crée un compte gratuit sur https://unsplash.com/developers, crée une
# "application", puis copie sa "Access Key" ici. Sans cette clé, le site
# fonctionne quand même : il utilise simplement des images de secours
# (placeholder avec le nom du produit) à la place de vraies photos.
UNSPLASH_ACCESS_KEY=

# --- Resend (envoi d'email : bienvenue, confirmation commande, contact) ---
# Crée un compte gratuit sur https://resend.com, récupère une clé API
# (Dashboard > API Keys). Sans domaine vérifié, les emails partent quand
# même via l'adresse partagée onboarding@resend.dev (EMAIL_FROM ci-dessous).
# Sans cette clé, le site fonctionne quand même : les emails sont juste
# ignorés (log dans la console) au lieu d'être envoyés.
RESEND_API_KEY=
# EMAIL_FROM=TradeHub <commandes@tondomaine.com>   # une fois un domaine vérifié dans Resend
ADMIN_NOTIFICATION_EMAIL=t6535875@gmail.com

# --- Sécurité (optionnel en développement, recommandé en production) ---
# Si tu ne les définis pas, des valeurs par défaut de développement sont
# utilisées automatiquement (voir middleware/auth.js et utils/crypto.js).
JWT_SECRET=
ENCRYPTION_KEY=
